name: Build and Deploy code

on: [push, pull_request]

jobs:
  job1: 
    runs-on: ubuntu-latest
    steps:
      - name: Pull Git Repo
        uses: actions/Checkout@v2
      - name: Say Hi to Avalcor
        run: echo "Hello Avalcor"